{
  "name": "naya-onboarding-bot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test dist/tests/*.test.js",
    "pretest": "npm run build"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
